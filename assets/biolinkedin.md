👋 Ssr Full-Stack / AI Engineer, 4 years shipping multi-tenant SaaS and AI platforms end-to-end. Core: tenant isolation (Supabase RLS), POS/orders, RBAC, LangChain/LangGraph + offline RAG chatbots, face recognition in prod, AI document intelligence (OCR/NLP/STT), legacy DB migrations, security-first (MFA, CSRF, HMAC/JWT).



Daily stack: Next.js, React, TypeScript, Tailwind, Supabase/PostgreSQL, NestJS, Prisma, Python (FastAPI, InsightFace, Pandas), Docker, NGINX, AWS, Redis/BullMQ, SendGrid.



🧩 Key Platforms



1️⃣ Ocean Stack - Multi-tenant SaaS (Niappa POS + Oceans HR ATS). Niappa POS: table sessions, split checks, resilient order lifecycle, Mercado Pago. Oceans HR ATS (hundreds of CVs/month per tenant): Kanban, vacancies, funnel reports, ES/EN i18n. AI CV Matching: Gemini API (cascade), PDF extraction, scoring across 8 criteria. SendGrid transactional emails on candidate phase changes (auto + semi-auto, Supabase triggers, idempotency, audit log). Next.js 16, React 19, TanStack Query, @dnd-kit. 



2️⃣ SPF - Institutional Platform (Full-Stack + AI). Two-platform ecosystem (internal ops + public verification), 170 concurrent users. NL intelligence chatbot over 90+ table legacy DB: LangChain LCEL + LangGraph orchestration (structured output, streaming, feature-flag rollback, semantic-validation retries, map-reduce fan-out) on grounded local LLM (Ollama/Qwen2.5, CPU-only, air-gapped), SQL-RAG/offline RAG, golden-set evals, inline [n] citations; >45s self-join → sub-second. Python Face Matcher in prod: InsightFace buffalo_l, 512-d embeddings, cosine search, MariaDB BLOB (FastAPI). AI doc intelligence: OCR + STT + PDF extraction, human-in-the-loop. Legacy migration: ~110 GB iBase8 → MySQL/MariaDB, 80+ modules via ETL. Secure verification: HMAC-SHA256, JWT, watermarking. MFA/CSRF/rate limiting. Docker + NGINX + PM2 + Debian.

Live: Private Platforms



3️⃣ VirtuaState - Responsive VR/AR studio site (HTML5/CSS/JS, mobile-first), SEO optimized. Live: virtuastate.net



☁️ AWS + Terraform (IaC) — Personal. VPC, EC2 (Dockerized NestJS + NGINX), RDS MySQL, S3 + CloudFront. 

Live: d2hiu34pyeqif1.cloudfront.net



⚙️ React · Next.js · NestJS · TypeScript · Tailwind · Prisma · Supabase/PostgreSQL (RLS) · MySQL · MariaDB · FastAPI · LangChain · LangGraph · Local LLM (Ollama) · InsightFace · Gemini API · OCR/NLP/STT · Redis/BullMQ · SendGrid · AWS · Terraform · Docker · NGINX



🎯 Multi-tenant SaaS · RAG chatbots · Face recognition in prod · AI document intelligence · Legacy DB migrations · POS/Orders · Web security.


espanol:
👋 Ssr Full-Stack / AI Engineer, 4 años entregando SaaS multi-tenant y plataformas con IA end-to-end. Especialidad: aislamiento por tenant (Supabase RLS), POS/órdenes, RBAC, LangChain/LangGraph + RAG offline, reconocimiento facial en prod, IA documental (OCR/NLP/STT), migraciones legacy, seguridad (MFA, CSRF, HMAC/JWT).



Stack: Next.js, React, TypeScript, Tailwind, Supabase/PostgreSQL, NestJS, Prisma, Python (FastAPI, InsightFace, Pandas), Docker, NGINX, AWS, Redis/BullMQ, SendGrid.



🧩 Plataformas clave



1️⃣ Ocean Stack - SaaS multi-tenant (Niappa POS + Oceans HR ATS). Niappa POS: mesas, cuentas divididas, ciclo de órdenes resiliente, Mercado Pago. Oceans HR ATS (cientos de CVs/mes por cliente): Kanban, vacantes, reportes de embudo, i18n ES/EN. AI CV Matching: Gemini API (cascada), extracción de PDF, scoring sobre 8 criterios. Emails transaccionales SendGrid por cambio de fase (auto + semi-auto, triggers Supabase, idempotencia, log por tenant). Next.js 16, React 19, TanStack Query, @dnd-kit.



2️⃣ SPF - Plataforma institucional (Full-Stack + IA). Dos plataformas (ops interna + verificación pública), 170 usuarios concurrentes. Chatbot NL de inteligencia sobre DB legacy de 90+ tablas: LangChain LCEL + orquestación LangGraph (structured output, streaming, rollback por feature flag, reintentos con validación semántica, map-reduce fan-out) sobre LLM local grounded (Ollama/Qwen2.5, solo CPU, air-gapped), SQL-RAG/RAG offline, evals con golden set, citas [n]; self-join de >45s → sub-segundo. Python Face Matcher en prod: InsightFace buffalo_l, embeddings 512-d, búsqueda cosine, BLOB MariaDB (FastAPI). IA documental: OCR + STT + extracción de PDFs con revisión humana. Migración: ~110 GB iBase8 → MySQL/MariaDB, 80+ módulos vía ETL. Verificación: HMAC-SHA256, JWT, marca de agua. MFA/CSRF/rate limiting. Docker + NGINX + PM2 + Debian.



3️⃣ VirtuaState - Sitio responsive VR/AR (HTML5/CSS/JS, mobile-first), SEO. Live: virtuastate.net



☁️ AWS + Terraform (IaC) — Personal. VPC, EC2 (NestJS + NGINX dockerizados), RDS MySQL, S3 + CloudFront.



Live: d2hiu34pyeqif1.cloudfront.net



⚙️ React · Next.js · NestJS · TypeScript · Tailwind · Prisma · Supabase/PostgreSQL (RLS) · MySQL · MariaDB · FastAPI · LangChain · LangGraph · LLM local (Ollama) · InsightFace · Gemini API · OCR/NLP/STT · Redis/BullMQ · SendGrid · AWS · Terraform · Docker · NGINX



🎯 SaaS multi-tenant · Chatbots RAG · Face recognition en prod · IA documental · Migraciones de DB legacy · POS/Órdenes · Seguridad web.


ingles servicio penit federal: 
I lead architecture, development, security and deployment of nationwide institutional platforms for the SPF, full ownership of the stack: frontend, backend, AI services, Dockerized infra (Debian + NGINX + PM2, 170 concurrent users). Replacing manual processes with secure, AI-assisted solutions.

🔹 Institutional Platform Sige2i (Private – VPN)

End-to-end modernization replacing Excel workflows and legacy iBase8 systems (Next.js, NestJS, Prisma, MySQL/MariaDB).

Key contributions:

NL intelligence chatbot (LangChain LCEL + LangGraph, SQL-RAG + local LLM): deterministic SQL over a 90+ table legacy DB (zero hallucinated facts, air-gapped) + grounded local LLM (Ollama/Qwen2.5, CPU-only). LangChain: structured output, token streaming, byte-for-byte parity, feature-flagged rollback. LangGraph: semantic-validation retries (best-attempt), map-reduce fan-out. Golden-set A/B/C evals, inline [n] citations, NDJSON streaming; >45s self-join → sub-second.

Python Face Matcher in prod: InsightFace buffalo_l (ONNX CPU), 512-d embeddings, NumPy cosine top-K, per-UID centroid, MariaDB BLOB storage. Endpoints /face/embed, /face/search, /face/search-multi (FastAPI).

Legacy DB migration: ~110 GB iBase8 → MySQL/MariaDB across 80+ modules (tables + PDFs, images, ZIPs, Word). Normalized schema, ETL with chunked streaming, checksum integrity.

AI document intelligence (Python/FastAPI): OCR (Tesseract), hybrid PDF extraction (PyPDF2 + Poppler), ASR (faster-whisper, .opus via ffmpeg). Auto-populate forms with human-in-the-loop, summaries, risk classification.

PDF/Excel/Word reports with temporal and statistical filters.
GIS with Leaflet and Google Maps.

Security: Auth0 2FA, CSRF, rate limiting, Helmet/CORS. Deploy: Docker + NGINX + PM2, automated backups.

🔹 Public Web Verification Portal (External)

HMAC-SHA256, time-limited JWT, anti-copy/anti-capture with dynamic watermarking, TipTap JSON → HTML rendering. Docker/Compose, NGINX, Helmet, CORS.



espanol a pasar: 
Lidero arquitectura, desarrollo, seguridad y despliegue de plataformas institucionales de alcance nacional para el Servicio Penitenciario Federal, ownership completo del stack: frontend, backend, IA, infra dockerizada (Debian + NGINX). Reemplazo procesos manuales por soluciones seguras y asistidas por IA.

🔹 Plataforma Institucional de Eventos Complejos (Privada – Alcance Nacional)

Modernización de procesos internos, reemplazando Excel por plataforma web (Next.js, NestJS, Prisma, MySQL). Arquitectura modular, formularios dinámicos, carga y procesamiento de imágenes, reportes PDF/Excel/Word con filtros, GIS con Leaflet/Google Maps. Seguridad: Auth0 2FA, CSRF, rate limiting. Deploy: Docker, NGINX, backups automáticos MySQL.

Chatbot NL con LangChain LCEL + LangGraph, SQL-RAG + LLM local (Ollama/Qwen2.5, solo CPU, air-gapped, cero alucinaciones): structured output, streaming, rollback por feature flag, reintentos con validación semántica, map-reduce fan-out, evals golden-set, citas [n]; self-join >45s → sub-segundo.

Microservicio IA en Python/FastAPI: OCR + Speech-to-Text sobre imágenes/PDFs/audios, autocompletado con revisión humana, resúmenes con clasificación de riesgo.

🔹 Portal Público de Verificación Web (Externo)

Verificación segura de documentos: HMAC-SHA256, accesos temporales JWT, anticopia/anticaptura con watermark dinámico, renderizado TipTap JSON → HTML. Deploy: Docker/Compose, NGINX, Helmet, CORS.

🔹 Sistema Interno de Tickets de Mantenimiento

PHP + MySQL con Google SSO, auditoría completa e historial consultable.